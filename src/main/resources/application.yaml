spring:
  datasource:
    url: ${SPRING_DATASOURCE_URL}
    username: ${SPRING_DATASOURCE_USERNAME}
    password: ${SPRING_DATASOURCE_PASSWORD}
  liquibase:
    change-log: classpath:db/changelog/db.changelog-master.xml
  mvc:
    throw-exception-if-no-handler-found: true
  web:
    resources:
      add-mappings: false

ai-model:
  client:
    url: ${AI_MODEL_CLIENT_URL}

jwt:
  secret: ${JWT_SECRET_KEY:aD8@fh!2B9#xL35M$eKp1nRz6wX*3S9mG&L7qP0Z@cJ5hQ2VjN%o7tU8VrX$kT3Q}
  expiration: ${JWT_EXPIRATION:86400000} # по дефолту 24 часа

outside-requests:
  front: ${OUTSIDE_REQUESTS_FRONT:http://localhost:3000}